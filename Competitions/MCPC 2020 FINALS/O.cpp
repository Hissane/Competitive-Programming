#include <bits/stdc++.h>

using namespace std;

int main(){
    freopen("bar.in","r",stdin);
    ios::sync_with_stdio(false);
    cin.tie(0);

    int f, x; 
    cin >> f >> x; 
    long long int res = f*x;
    cout << res; 
    return 0;
}