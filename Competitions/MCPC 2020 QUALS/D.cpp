#include <bits/stdc++.h>

using namespace std;

//not complete

int main(){
    freopen("text.in","r",stdin);
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    int n;
    cin >> n;
    for(int i = 0; i < n; i++){
        int w;
        double h;
        double bmi;
        cin >> w >> h; 
        bmi  = w / h*h;
        if(bmi < 19){
            double goal = 19*h*h
        }else if(bmi > 25){

        }else{

        }
    }

    return 0;
}